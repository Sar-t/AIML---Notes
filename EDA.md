# Exploratory Data Analysis

## Matplotlib

- plt.scatter(vector1,vector2) : creates a  graph showing all the 2d points

- if we want to distinguish based on value of third column
plt.scatter(vector1,vector2,c=vector3) // color codes will be used